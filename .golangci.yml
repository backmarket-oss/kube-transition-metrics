issues:
  exclude-use-default: false
linters:
  enable-all: true
  disable:
    - nosnakecase
    - paralleltest
    - golint
    - maligned
    - scopelint
    - varcheck
    - interfacer
    - ifshort
    - exhaustivestruct
    - exhaustruct
    - structcheck
    - deadcode
    - gofumpt
    - wsl
    - godox
    - ireturn
    - testpackage
linters-settings:
  stylecheck:
    checks: ["all", "-ST1000"]
  revive:
    rules:
      - name: exported
        disabled: false
